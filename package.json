{
	"name": "vault-steward",
	"version": "0.1.0",
	"description": "AI-powered assistant that creates intelligent connections between notes via links, tags, and organization.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"ai",
		"claude",
		"linking",
		"tagging"
	],
	"author": "Cheppy",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"esbuild": "^0.19.11",
		"tslib": "^2.6.2",
		"typescript": "^5.3.3"
	},
	"dependencies": {
		"obsidian": "latest"
	}
}
